@model app.Models.Entities.FonteDeEnergia

@{
    Layout = "_ContentLayout";
    ViewData["Title"] = "Excluir Fonte de Energia";
}

<h2>Excluir Fonte de Energia</h2>

<h3>Tem certeza de que deseja excluir esta fonte de energia?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Tipo)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Tipo)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EficienciaMedia)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EficienciaMedia)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Excluir" class="btn btn-danger" /> |
        <a asp-action="Index">Voltar para a lista</a>
    </form>
</div>