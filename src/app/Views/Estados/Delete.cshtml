@model app.Models.Entities.Estado

@{
    ViewData["Title"] = "Excluir Estado";
}

<h2>Excluir Estado</h2>

<div>
    <h4>Tem certeza que deseja excluir o estado @Model.Name?</h4>
    <dl class="row">
        <dt class="col-sm-2">Nome</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Sigla</dt>
        <dd class="col-sm-10">@Model.Sigla</dd>

        <dt class="col-sm-2">Data de Criação</dt>
        <dd class="col-sm-10">@Model.CreatedAt.ToString("dd/MM/yyyy")</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Excluir</button>
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
